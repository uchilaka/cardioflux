CardioFlux.controller('WorkoutCtrl', [
    '$rootScope', '$scope',
    function($rootScope, $scope) {
        console.log("[module.controller.Workout]");
        
        $scope.init = function() {
            
        };
        $scope.init();
        
    }
]);